<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.otterlibrary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OtterLibrary">
        <activity
            android:name=".login"
            android:exported="false"
            android:label="@string/Login_title"/>
        <activity
            android:name=".placeholdManage"
            android:exported="false"
            android:label="@string/placeholder_title"/>
        <activity
            android:name=".ManageSystem"
            android:exported="false"
            android:label="@string/manage_system_title" />
        <activity
            android:name=".CreateAccountManage"
            android:exported="false"
            android:label="@string/Create_account_title" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>